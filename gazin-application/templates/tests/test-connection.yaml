apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gazin-application.fullname" . }}-test-connection"
  labels:
    {{- include "gazin-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gazin-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
