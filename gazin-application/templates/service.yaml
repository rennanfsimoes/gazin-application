apiVersion: v1
kind: Service
metadata:
  name: {{ include "gazin-application.fullname" . }}
  labels:
    {{- include "gazin-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gazin-application.selectorLabels" . | nindent 4 }}
